function [U,traj] = get_traj(init,target,r,d,dt)
%init:Initial state
%traget:target state
%r: wheel radius or 20
%d: robot width or 85
%dt: sampling time
%U: Nx2 matrix representing set of control inputs with each row being one u
%traj: set of trajectory with each row being one x


end

